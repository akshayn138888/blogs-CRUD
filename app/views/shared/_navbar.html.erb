<nav class="navbar navbar-light bg-light">
    <div style="width: 20%;">
      <a class="navbar-brand text-primary" href = "/">Blog On Rails</a>
    </div>
    <div style="width: 80%;">
      <div class="float-right" style="width: 25%;">
        <div class="d-flex justify-content-around align-items-end">
          <a href="/">
            <h5 class="nav-item text-primary">Home</h5>
          </a>
          <a href="/posts/new">
            <h5 class="nav-item text-primary">New Post</h5>
          </a>
     <% if user_signed_in? %>
      <%= link_to "Logout", session_path, method: :delete, class: 'btn btn-danger' %>
      <%= link_to "#{current_user.name}", edit_user_path(current_user), class: 'btn btn-primary'%>
     <% else %>    
      <%= link_to "Log In", new_session_path, class: 'btn btn-primary'%>
      <%= link_to "Sign Up", new_user_path, class: 'btn btn-warning' %>
     <% end %>
       </div>
      </div>
    </div>
  </nav>










