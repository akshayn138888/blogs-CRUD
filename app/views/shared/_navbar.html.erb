<nav class="navbar navbar-light bg-light">
    <div style="width: 25%;">
      <a class="navbar-brand text-primary" href = "/">Blog On Rails</a>
    </div>
    <div style="width: 75%;">
      <div class="float-right" style="width: 15%;">
        <div class="d-flex justify-content-around">
          <a href="/">
            <h5 class="nav-item text-primary">Home</h5>
          </a>
          <a href="/posts/new">
            <h5 class="nav-item text-primary">New Post</h5>
          </a>
           <div>
   <% if user_signed_in? %>
      <%= link_to "Logout", session_path, method: :delete, class: 'btn btn-primary' %>
      <%= current_user.name %> 
     <% else %>    
      <%= link_to "Log In", new_session_path, class: 'btn btn-success'%>
     <% end %>
  </div>
       </div>
      </div>
    </div>
  </nav>










